<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dualdatasources.db.mapper.CoreIdMapper">


    <select id="selectById" parameterType="Integer" resultType="dualdatasources.db.entity.CoreIdDO">
        SELECT
            id, full_name as fullName,id_num as idNum
        FROM coreid
    </select>

</mapper>
